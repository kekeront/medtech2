# Base URL of the MedArchive FastAPI backend (uvicorn app.api:app).
# The Next dev server proxies /api/* to this (see next.config.mjs).
# Copy to .env.local and adjust to your backend port.
BACKEND_URL=http://localhost:8000
